const maiorNumero = (x, y) => {
  return x > y ? x : y;
}

const maiorNumero2 = (x, y) => {
  return x > y ? x : y;
}

const maiorNumero3 = (x, y) => x > y ? x : y;


console.log(maiorNumero3(100, 20));
