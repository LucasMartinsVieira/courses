describe('Testing something', () => {
  it('it should return one', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('Testing another thing', () => {
  test('it should return Lucas', () => {
    const name = 'Lucas';
    expect(name).toBe('Lucas');
  });
});
